# IndiaCryptoAlpha - Linux Full Features (Updated for Python 3.13)
# Python 3.11+ REQUIRED
# Platform: Linux (x86_64, WSL2)
# Last Updated: April 2026
# ============================================================================

# Core dependencies (with Python 3.13 fixes)
-r requirements.txt

# ============================================================================
# LINUX-SPECIFIC ENHANCEMENTS
# ============================================================================

# Scientific Computing (good wheels on Linux for Python 3.13)
scipy>=1.14.0

# Machine Learning (supports Python 3.13)
scikit-learn>=1.6.0

# ============================================================================
# DASHBOARD & VISUALIZATION (Streamlit + extras)
# ============================================================================
streamlit>=1.55.0          # Latest stable as of April 2026
plotly>=5.24.0             # Interactive charts (commonly used with Streamlit)
altair>=5.0.0              # Declarative visualization (optional but useful)

# ============================================================================
# OPTIONAL ADVANCED FEATURES (Uncomment if you need them)
# ============================================================================
# ta-lib==0.4.28           # Requires system TA-Lib library installed via apt
# tensorflow>=2.17.0       # Deep learning (heavy, needs GPU for best performance)
# torch>=2.4.0 torchvision>=0.19.0  # PyTorch (CPU version by default)

# ============================================================================
# NOTES FOR LINUX USERS
# ============================================================================
# This file is optimized for Kali Linux / WSL2 with Python 3.13
# Key fixes:
#   - numpy >=2.1.0 and pandas >=2.2.3 (in requirements.txt) → fixes build error
#   - scipy and scikit-learn use modern versions with Python 3.13 wheels
#   - Streamlit added for the dashboard folder
#
# Installation:
#   source venv/bin/activate
#   pip install -r requirements-linux.txt
#
# If you face any wheel/compilation issues, run:
#   sudo apt update && sudo apt install -y build-essential gfortran python3-dev
